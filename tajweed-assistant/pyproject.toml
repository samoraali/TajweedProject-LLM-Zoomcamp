[project]
name = "tajweed-assistant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samar Ali", email = "samar.eldemeiri@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
]

[project.scripts]
tajweed-assistant = "tajweed_assistant:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
